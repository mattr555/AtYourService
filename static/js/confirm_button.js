// Generated by CoffeeScript 1.6.3
(function() {
  jQuery(function() {
    $('.button-div').on('click', '.confirm-button.btn-success', function() {
      var button, event_id, response, user_id;
      console.log("clicked!");
      button = $(this);
      user_id = button.data('user-id');
      event_id = button.data('event-id');
      response = $.ajax('/ajax/main/confirm_participant.json', {
        data: {
          'user_id': user_id,
          'event_id': event_id
        },
        success: function(data) {
          button.removeClass('btn-success').addClass(data.data.button_class).html(data.data.button_text);
          button.parents('tr').removeClass().addClass(data.data.row_class);
          return button.parents('tr').children('.status-field').html(data.data.status);
        },
        timeout: 3000,
        type: "POST"
      });
      return void 0;
    });
    return void 0;
  });

}).call(this);
